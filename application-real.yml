spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://dc-mysql:3306/testing
    # Hide this before deploying on AWS!
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

# 바인딩 파라미터 확인
logging:
  level:
    org:
      hibernate:
        orm:
          jdbc:
            bind: trace